<div class="modal-header"><h3>Обратная связь</h3></div>
<br>
<p>Если у вас имеются вопросы или предложения к администрации сайта, то вы можете воспользоваться
контактной формой - мы всегда открыты для помощи и интересной беседы.</p>

<div class="formContainer">
  <form class="form-horizontal newsletterForm" role="form" action="/process?form=contact" method="POST">
    <input type="hidden" name="_csrf" value="{{csrf}}">
    <div class="form-group">
      <div class="col-sm-6 control-label"><label for="fieldName">Имя</label></div>
      <div class="col-sm-6">
        <input type="text" class="form-control" id="fieldName" name="name">
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-6 control-label"><label for="fieldEmail">Email</label></div>
      <div class="col-sm-6">
        <input type="email" class="form-control" required id="fieldEmail" name="email">
      </div>
    </div>
    <div class="form-group">
      <div class="col-sm-6 control-label"><label for="fieldEmail">Сообщение</label></div>
      <div class="col-sm-6">
        <textarea name="textform" rows="8" class="form-control" required id="fieldtext" cols="80"></textarea>
      </div>
    </div>
    <div class="form-group">
   <div class="col-sm-offset-2 col-sm-6">
     <button type="submit" class="btn btn-default" >Отправить</button>
   </div>
  </div>
 </form>
</div>

 {{#section 'jquery'}}
 <script>
    $(document).ready(function(){
    $('.contactForm'). on('submit', function(evt){
         evt. preventDefault();
    var action = $(this).attr('action' );
    var $container = $(this).closest('.formContainer' );
         $. ajax({
             url: action,
              type: 'POST' ,
               data: $(this). serialize(),
      success: function(data){
               if(data.success){
              $container.html('<h2>Спасибо!</h2>' );
                } else {
                   $container.html('Возникла проблема.' );
                 }       },
       error: function(){
                  $container.html('Возникла проблема.' );
          }      });  });  });
  </script>
{{/section}}
